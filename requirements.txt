# Traffic Orchestrator — simplified dependencies
# Removed: celery, kombu, sqlalchemy, redis, amqp, billiard
# Added: psycopg (v3, psycopg2-binary as fallback)

fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Postgres driver — psycopg3 with binary wheels
psycopg[binary]>=3.1.0

# HTTP client for dispatcher (sync, runs in threadpool)
requests>=2.31.0

# Data validation
pydantic>=2.7.0
