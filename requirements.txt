fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Postgres driver for app/storage.py (psycopg v3)
psycopg[binary]>=3.1.0

# HTTP client for dispatcher (sync, runs in thread pool)
requests>=2.31.0

# Data validation
pydantic>=2.7.0

