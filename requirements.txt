fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Postgres driver for app/storage.py (psycopg v3)
psycopg[binary]>=3.1.0

# Celery + broker dependencies
celery>=5.3.0
kombu>=5.3.0
sqlalchemy>=1.4.0,<2.0
psycopg2-binary>=2.9.0

# HTTP client for dispatcher (sync, runs in worker process)
requests>=2.31.0

# Data validation
pydantic>=2.7.0
